# Copy to .env.local to connect the frontend to the backend (server/).
# Leave unset to run MyFam in its original local-demo mode (no login, no payments).
VITE_API_BASE=https://myfam-api.YOUR-SUBDOMAIN.workers.dev
